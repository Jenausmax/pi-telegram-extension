# Реальный конфиг с секретами — не коммитим (в репозитории только .env.example)
.env
# Локальные данные рантайма (на сервере)
workspace/
sessions/
exports/
state.json

# Зависимости и сборка
node_modules/
dist/
